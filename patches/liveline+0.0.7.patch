diff --git a/node_modules/liveline/dist/index.cjs b/node_modules/liveline/dist/index.cjs
index 4709730..9124718 100644
--- a/node_modules/liveline/dist/index.cjs
+++ b/node_modules/liveline/dist/index.cjs
@@ -387,7 +387,7 @@ function blendColor(c1, c2, t) {
   if (a >= 0.995) return `rgb(${r},${g},${b})`;
   return `rgba(${r},${g},${b},${a.toFixed(3)})`;
 }
-function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlpha = 1, strokeColor) {
+function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlpha = 1, strokeColor, linear = false) {
   const { h, pad } = layout;
   const baseAlpha = ctx.globalAlpha;
   if (showFill && fillAlpha > 0.01) {
@@ -398,7 +398,13 @@ function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlp
     ctx.beginPath();
     ctx.moveTo(pts[0][0], h - pad.bottom);
     ctx.lineTo(pts[0][0], pts[0][1]);
-    drawSpline(ctx, pts);
+    if (linear) {
+      for (let i = 1; i < pts.length; i++) {
+        ctx.lineTo(pts[i][0], pts[i][1]);
+      }
+    } else {
+      drawSpline(ctx, pts);
+    }
     ctx.lineTo(pts[pts.length - 1][0], h - pad.bottom);
     ctx.closePath();
     ctx.fillStyle = grad;
@@ -407,15 +413,99 @@ function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlp
   ctx.globalAlpha = baseAlpha * lineAlpha;
   ctx.beginPath();
   ctx.moveTo(pts[0][0], pts[0][1]);
-  drawSpline(ctx, pts);
-  ctx.strokeStyle = strokeColor ?? palette.line;
+  if (linear) {
+    for (let i = 1; i < pts.length; i++) {
+      ctx.lineTo(pts[i][0], pts[i][1]);
+    }
+  } else {
+    drawSpline(ctx, pts);
+  }
+  const _lineDash = palette.lineStyle === 'dashed' ? [8, 5] : palette.lineStyle === 'dotted' ? [2, 4] : [];
+  ctx.setLineDash(_lineDash);
+  if (palette.gradientStops && palette.gradientStops.length >= 2) {
+    const _grad = ctx.createLinearGradient(layout.pad.left, 0, layout.pad.left + layout.chartW, 0);
+    palette.gradientStops.forEach((c, i) => _grad.addColorStop(i / (palette.gradientStops.length - 1), c));
+    ctx.strokeStyle = _grad;
+  } else {
+    ctx.strokeStyle = strokeColor ?? palette.line;
+  }
   ctx.lineWidth = palette.lineWidth;
   ctx.lineJoin = "round";
   ctx.lineCap = "round";
   ctx.stroke();
+  ctx.setLineDash([]);
   ctx.globalAlpha = baseAlpha;
 }
-function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scrubX, scrubAmount = 0, chartReveal = 1, now_ms = 0, colorBlend = 1, skipDashLine = false, fillScale = 1) {
+var SESSION_DIM_ALPHA = 0.35;
+var SESSION_FADE_SECS = 0;
+function renderCurveWithSessions(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor, linear, sessions) {
+  if (!sessions) {
+    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor, linear);
+    return;
+  }
+  const { pad, toX, h, chartH } = layout;
+  const baseAlpha = ctx.globalAlpha;
+  const resolvedColor = strokeColor ?? palette.line;
+  const [r, g, b] = parseRgba(resolvedColor);
+  const dimColor = `rgba(${r},${g},${b},${SESSION_DIM_ALPHA})`;
+  const fullColor = resolvedColor;
+  const fadeW = (toX(SESSION_FADE_SECS) - toX(0));
+  const drawPath = (color, alpha) => {
+    ctx.save();
+    ctx.globalAlpha = baseAlpha * alpha;
+    ctx.beginPath();
+    ctx.moveTo(pts[0][0], pts[0][1]);
+    if (linear) {
+      for (let i = 1; i < pts.length; i++) ctx.lineTo(pts[i][0], pts[i][1]);
+    } else {
+      drawSpline(ctx, pts);
+    }
+    const _dp_dash = palette.lineStyle === 'dashed' ? [8, 5] : palette.lineStyle === 'dotted' ? [2, 4] : [];
+    ctx.setLineDash(_dp_dash);
+    ctx.strokeStyle = color;
+    ctx.lineWidth = palette.lineWidth;
+    ctx.lineJoin = "round";
+    ctx.lineCap = "round";
+    ctx.stroke();
+    ctx.setLineDash([]);
+    ctx.restore();
+  };
+  const clipRect = (x, w, fn) => {
+    ctx.save();
+    ctx.beginPath();
+    ctx.rect(x, pad.top, w, chartH);
+    ctx.clip();
+    fn();
+    ctx.restore();
+  };
+  if (sessions.length === 0) {
+    clipRect(pad.left - 1, layout.w, () => drawPath(dimColor, lineAlpha));
+    return;
+  }
+  clipRect(pad.left - 1, layout.w, () => drawPath(dimColor, lineAlpha));
+  for (const sess of sessions) {
+    const x1 = toX(sess.start);
+    const x2 = toX(sess.end);
+    const coreX1 = x1 + fadeW;
+    const coreX2 = x2 - fadeW;
+    if (coreX1 < coreX2) {
+      clipRect(coreX1, coreX2 - coreX1, () => drawPath(fullColor, lineAlpha));
+    }
+    clipRect(x1 - fadeW, 2 * fadeW, () => {
+      const grad = ctx.createLinearGradient(x1 - fadeW, 0, x1 + fadeW, 0);
+      grad.addColorStop(0, dimColor);
+      grad.addColorStop(1, fullColor);
+      drawPath(grad, lineAlpha);
+    });
+    clipRect(x2 - fadeW, 2 * fadeW, () => {
+      const grad = ctx.createLinearGradient(x2 - fadeW, 0, x2 + fadeW, 0);
+      grad.addColorStop(0, fullColor);
+      grad.addColorStop(1, dimColor);
+      drawPath(grad, lineAlpha);
+    });
+  }
+}
+function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scrubX, scrubAmount = 0, chartReveal = 1, now_ms = 0, colorBlend = 1, skipDashLine = false, fillScale = 1, extendToNow = true, linear = false, sessions = null) {
   const { h, pad, toX, toY, chartW, chartH } = layout;
   const incomingAlpha = ctx.globalAlpha;
   const yMin = pad.top;
@@ -431,15 +521,19 @@ function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scr
     const baseY = loadingY(t, centerY, amplitude, scroll);
     return baseY + (rawY - baseY) * localReveal;
   } : (rawY, _x) => rawY;
+  const doLiveTail = extendToNow !== false;
   const pts = visible.map((p, i) => {
     const x = toX(p.time);
-    const y = i === visible.length - 1 ? morphY(clampY(toY(smoothValue)), x) : morphY(clampY(toY(p.value)), x);
+    const useSmoothLast = doLiveTail && i === visible.length - 1;
+    const y = useSmoothLast ? morphY(clampY(toY(smoothValue)), x) : morphY(clampY(toY(p.value)), x);
     return [x, y];
   });
-  const liveTipX = toX(now);
-  const fullRightX = pad.left + chartW;
-  const tipX = chartReveal < 1 ? liveTipX + (fullRightX - liveTipX) * (1 - chartReveal) : liveTipX;
-  pts.push([tipX, morphY(clampY(toY(smoothValue)), tipX)]);
+  if (doLiveTail) {
+    const liveTipX = toX(now);
+    const fullRightX = pad.left + chartW;
+    const tipX = chartReveal < 1 ? liveTipX + (fullRightX - liveTipX) * (1 - chartReveal) : liveTipX;
+    pts.push([tipX, morphY(clampY(toY(smoothValue)), tipX)]);
+  }
   if (pts.length < 2) return;
   let lineAlpha = 1;
   let fillAlpha = fillScale;
@@ -451,6 +545,7 @@ function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scr
   const colorT = Math.min(1, chartReveal * 3) * colorBlend;
   const strokeColor = chartReveal < 1 || colorBlend < 1 ? blendColor(palette.gridLabel, palette.line, colorT) : void 0;
   const isScrubbing = scrubX !== null;
+  const renderFn = sessions ? (sc, la, fa) => renderCurveWithSessions(ctx, layout, palette, pts, showFill, la, fa, sc, linear, sessions) : (sc, la, fa) => renderCurve(ctx, layout, palette, pts, showFill, la, fa, sc, linear);
   ctx.save();
   ctx.beginPath();
   ctx.rect(pad.left - 1, pad.top, chartW + 2, chartH);
@@ -460,17 +555,17 @@ function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scr
     ctx.beginPath();
     ctx.rect(0, 0, scrubX, h);
     ctx.clip();
-    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor);
+    renderFn(strokeColor, lineAlpha, fillAlpha);
     ctx.restore();
     ctx.save();
     ctx.beginPath();
     ctx.rect(scrubX, 0, layout.w - scrubX, h);
     ctx.clip();
     ctx.globalAlpha = incomingAlpha * (1 - scrubAmount * 0.6);
-    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor);
+    renderFn(strokeColor, lineAlpha, fillAlpha);
     ctx.restore();
   } else {
-    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor);
+    renderFn(strokeColor, lineAlpha, fillAlpha);
   }
   ctx.restore();
   if (!skipDashLine) {
@@ -532,11 +627,12 @@ function drawDot(ctx, x, y, palette, pulse = true, scrubAmount = 0, now_ms = per
   ctx.globalAlpha = baseAlpha;
   ctx.beginPath();
   ctx.arc(x, y, 3.5, 0, Math.PI * 2);
+  const dotFill = palette.dotFlat ?? palette.line;
   if (dim > 0.01) {
-    const lineRgb = parseColorRgb(palette.line);
+    const lineRgb = parseColorRgb(dotFill);
     ctx.fillStyle = lerpColor(lineRgb, outerRgb, dim);
   } else {
-    ctx.fillStyle = palette.line;
+    ctx.fillStyle = dotFill;
   }
   ctx.fill();
 }
@@ -637,6 +733,9 @@ function drawCrosshair(ctx, layout, palette, hoverX, hoverValue, hoverTime, form
   const valueText = formatValue(hoverValue);
   const timeText = formatTime(hoverTime);
   const separator = "  \xB7  ";
+  const vNum = Number(hoverValue);
+  const valueFill =
+    vNum > 0 ? "#22c55e" : vNum < 0 ? "#ef4444" : palette.tooltipText;
   ctx.save();
   ctx.globalAlpha = scrubOpacity;
   ctx.font = '400 13px "SF Mono", Menlo, monospace';
@@ -659,7 +758,7 @@ function drawCrosshair(ctx, layout, palette, hoverX, hoverValue, hoverTime, form
     ctx.strokeText(valueText, tx, ty);
     ctx.strokeText(separator + timeText, tx + valueW, ty);
   }
-  ctx.fillStyle = palette.tooltipText;
+  ctx.fillStyle = valueFill;
   ctx.fillText(valueText, tx, ty);
   ctx.fillStyle = palette.gridLabel;
   ctx.fillText(separator + timeText, tx + valueW, ty);
@@ -688,66 +787,6 @@ function drawMultiCrosshair(ctx, layout, palette, hoverX, hoverTime, entries, fo
       ctx.fill();
     }
   }
-  if (scrubOpacity < 0.1 || layout.w < 300) return;
-  ctx.save();
-  ctx.globalAlpha = scrubOpacity;
-  ctx.font = '400 13px "SF Mono", Menlo, monospace';
-  ctx.textAlign = "left";
-  const timeText = formatTime(hoverTime);
-  const sep = "  \xB7  ";
-  const dotInline = " ";
-  const segments = [
-    { text: timeText, color: palette.gridLabel }
-  ];
-  for (const e of entries) {
-    segments.push({ text: sep, color: palette.gridLabel });
-    segments.push({ text: dotInline, color: e.color, isDot: true });
-    const label = e.label ? `${e.label} ` : "";
-    if (label) segments.push({ text: label, color: palette.gridLabel });
-    segments.push({ text: formatValue(e.value), color: palette.tooltipText });
-  }
-  let totalW = 0;
-  const segWidths = [];
-  for (const seg of segments) {
-    const w = seg.isDot ? 12 : ctx.measureText(seg.text).width;
-    segWidths.push(w);
-    totalW += w;
-  }
-  let tx = hoverX - totalW / 2;
-  const minX = pad.left + 4;
-  const dotRightEdge = liveDotX != null ? liveDotX + 7 : layout.w - pad.right;
-  const maxX = dotRightEdge - totalW;
-  if (tx < minX) tx = minX;
-  if (tx > maxX) tx = maxX;
-  const ty = pad.top + (tooltipY ?? 14) + 10;
-  if (tooltipOutline !== false) {
-    ctx.strokeStyle = palette.tooltipBg;
-    ctx.lineWidth = 3;
-    ctx.lineJoin = "round";
-    let ox2 = tx;
-    for (let i = 0; i < segments.length; i++) {
-      const seg = segments[i];
-      if (!seg.isDot) {
-        ctx.strokeText(seg.text, ox2, ty);
-      }
-      ox2 += segWidths[i];
-    }
-  }
-  let ox = tx;
-  for (let i = 0; i < segments.length; i++) {
-    const seg = segments[i];
-    if (seg.isDot) {
-      ctx.beginPath();
-      ctx.arc(ox + 4, ty - 4, 3.5, 0, Math.PI * 2);
-      ctx.fillStyle = seg.color;
-      ctx.fill();
-    } else {
-      ctx.fillStyle = seg.color;
-      ctx.fillText(seg.text, ox, ty);
-    }
-    ox += segWidths[i];
-  }
-  ctx.restore();
 }
 
 // src/draw/referenceLine.ts
@@ -1351,12 +1390,15 @@ function drawLineModeCrosshair(ctx, layout, palette, hoverX, value, hoverTime, f
   if (opacity < 0.1 || layout.w < 200) return;
   const val = formatValue(value);
   const time = formatTime(hoverTime);
+  const vNum = Number(value);
+  const valCol =
+    vNum > 0 ? "#22c55e" : vNum < 0 ? "#ef4444" : palette.line;
   ctx.save();
   ctx.globalAlpha = opacity;
   ctx.font = '400 13px "SF Mono", Menlo, monospace';
   ctx.textAlign = "left";
   const parts = [
-    { text: val, color: palette.line },
+    { text: val, color: valCol },
     { text: "  \xB7  ", color: palette.gridLabel },
     { text: time, color: palette.gridLabel }
   ];
@@ -1572,7 +1614,7 @@ function drawFrame(ctx, layout, palette, opts) {
     const lastPt = pts[pts.length - 1];
     const distToLive = lastPt[0] - opts.hoverX;
     const fadeStart = Math.min(80, layout.chartW * 0.3);
-    const scrubOpacity = distToLive < CROSSHAIR_FADE_MIN_PX ? 0 : distToLive >= fadeStart ? opts.scrubAmount : (distToLive - CROSSHAIR_FADE_MIN_PX) / (fadeStart - CROSSHAIR_FADE_MIN_PX) * opts.scrubAmount;
+    const scrubOpacity = opts.scrubAmount;
     if (scrubOpacity > 0.01) {
       drawCrosshair(
         ctx,
@@ -1627,6 +1669,9 @@ function drawMultiFrame(ctx, layout, opts) {
     if (combinedAlpha < 0.01) continue;
     ctx.save();
     if (combinedAlpha < 1) ctx.globalAlpha = combinedAlpha;
+    const ext = s.extendToNow !== false;
+    const lin = s.linear === true;
+    const sess = s.sessions ?? null;
     const pts = drawLine(
       ctx,
       layout,
@@ -1639,7 +1684,13 @@ function drawMultiFrame(ctx, layout, opts) {
       scrubX,
       opts.scrubAmount,
       reveal,
-      opts.now_ms
+      opts.now_ms,
+      1,
+      true,
+      1,
+      ext,
+      lin,
+      sess
     );
     ctx.restore();
     if (pts && pts.length > 0) {
@@ -1661,17 +1712,18 @@ function drawMultiFrame(ctx, layout, opts) {
     for (const entry of allPts) {
       if (entry.alpha < 0.01) continue;
       const lastPt = entry.pts[entry.pts.length - 1];
+      const dotFill = entry.palette.dotFlat ?? entry.palette.line;
       ctx.save();
       ctx.globalAlpha = dotAlpha * entry.alpha;
       if (showPulse && entry.alpha > 0.5) {
-        drawMultiDot(ctx, lastPt[0], lastPt[1], entry.palette.line, true, opts.now_ms, 3);
+        drawMultiDot(ctx, lastPt[0], lastPt[1], dotFill, true, opts.now_ms, 3);
       } else {
-        drawSimpleDot(ctx, lastPt[0], lastPt[1], entry.palette.line, 3);
+        drawSimpleDot(ctx, lastPt[0], lastPt[1], dotFill, 3);
       }
       if (entry.label) {
         ctx.font = '600 10px -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif';
         ctx.textAlign = "left";
-        ctx.fillStyle = entry.palette.line;
+        ctx.fillStyle = dotFill;
         ctx.fillText(entry.label, lastPt[0] + 6, lastPt[1] + 3.5);
       }
       ctx.restore();
@@ -1694,7 +1746,7 @@ function drawMultiFrame(ctx, layout, opts) {
     }
     const distToLive = maxLiveDotX - opts.hoverX;
     const fadeStart = Math.min(80, layout.chartW * 0.3);
-    const scrubOpacity = distToLive < CROSSHAIR_FADE_MIN_PX ? 0 : distToLive >= fadeStart ? opts.scrubAmount : (distToLive - CROSSHAIR_FADE_MIN_PX) / (fadeStart - CROSSHAIR_FADE_MIN_PX) * opts.scrubAmount;
+    const scrubOpacity = opts.scrubAmount;
     if (scrubOpacity > 0.01) {
       drawMultiCrosshair(
         ctx,
@@ -1990,8 +2042,8 @@ var VALUE_SNAP_THRESHOLD = 1e-3;
 var ADAPTIVE_SPEED_BOOST = 0.2;
 var MOMENTUM_GREEN = [34, 197, 94];
 var MOMENTUM_RED = [239, 68, 68];
-var CHART_REVEAL_SPEED = 0.14;
-var CHART_REVEAL_SPEED_FWD = 0.09;
+var CHART_REVEAL_SPEED = 1;
+var CHART_REVEAL_SPEED_FWD = 1;
 var PAUSE_PROGRESS_SPEED = 0.12;
 var PAUSE_CATCHUP_SPEED = 0.08;
 var PAUSE_CATCHUP_SPEED_FAST = 0.22;
@@ -2611,7 +2663,10 @@ function useLivelineEngine(canvasRef, containerRef, config) {
           data: s.data.slice(),
           value: s.value,
           palette: s.palette,
-          label: s.label
+          label: s.label,
+          extendToNow: s.extendToNow !== false,
+          linear: s.linear === true,
+          sessions: s.sessions ?? null
         }));
       }
       if (hasData && !cfg.isMultiSeries) lastMultiSeriesRef.current = [];
@@ -2660,8 +2715,8 @@ function useLivelineEngine(canvasRef, containerRef, config) {
     }
     if (isCandle) {
       const candleBuffer = CANDLE_BUFFER_NO_BADGE;
-      if (hasData) frozenNowRef.current = Date.now() / 1e3 - timeDebtRef.current;
-      const now = hasData || chartReveal < 5e-3 ? Date.now() / 1e3 - timeDebtRef.current : frozenNowRef.current;
+      if (hasData) frozenNowRef.current = Date.now() / 1e3 - timeDebtRef.current - (cfg.nowOffset ?? 0);
+      const now = hasData || chartReveal < 5e-3 ? Date.now() / 1e3 - timeDebtRef.current - (cfg.nowOffset ?? 0) : frozenNowRef.current;
       const rawLive = pausedCandlesRef.current ? pausedLiveRef.current ?? void 0 : cfg.liveCandle;
       let effectiveLineData = pausedLineDataRef.current ?? cfg.lineData;
       let effectiveLineValue = pausedLineValueRef.current ?? cfg.lineValue;
@@ -3087,8 +3142,8 @@ function useLivelineEngine(canvasRef, containerRef, config) {
       }
       const firstSeries = effectiveMultiSeries[0];
       const transition = windowTransitionRef.current;
-      if (hasData) frozenNowRef.current = Date.now() / 1e3 - timeDebtRef.current;
-      const now = useMultiStash ? frozenNowRef.current : Date.now() / 1e3 - timeDebtRef.current;
+      if (hasData) frozenNowRef.current = Date.now() / 1e3 - timeDebtRef.current - (cfg.nowOffset ?? 0);
+      const now = useMultiStash ? frozenNowRef.current : Date.now() / 1e3 - timeDebtRef.current - (cfg.nowOffset ?? 0);
       const smoothValues = /* @__PURE__ */ new Map();
       for (const s of effectiveMultiSeries) {
         let dv = displayValuesRef.current.get(s.id);
@@ -3181,7 +3236,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
             if (range.min < globalMin) globalMin = range.min;
             if (range.max > globalMax) globalMax = range.max;
           }
-          seriesEntries.push({ visible, smoothValue: sv, palette: s.palette, label: s.label, alpha });
+          seriesEntries.push({ visible, smoothValue: sv, palette: s.palette, label: s.label, alpha, extendToNow: s.extendToNow !== false, linear: s.linear === true, sessions: s.sessions ?? null });
         }
       }
       if (seriesEntries.length === 0) {
@@ -3203,7 +3258,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
         rafRef.current = requestAnimationFrame(draw);
         return;
       }
-      const computedRange = { min: isFinite(globalMin) ? globalMin : 0, max: isFinite(globalMax) ? globalMax : 1 };
+      const computedRange = cfg.yDomain ? { min: cfg.yDomain.min, max: cfg.yDomain.max } : { min: isFinite(globalMin) ? globalMin : 0, max: isFinite(globalMax) ? globalMax : 1 };
       const adaptiveSpeed = cfg.lerpSpeed + ADAPTIVE_SPEED_BOOST * 0.5;
       const rangeResult = updateRange(
         computedRange,
@@ -3332,8 +3387,8 @@ function useLivelineEngine(canvasRef, containerRef, config) {
       const needsArrowRoom = cfg.showMomentum && cfg.showBadge;
       const buffer = needsArrowRoom ? Math.max(baseBuffer, 37 / Math.max(chartW, 1)) : baseBuffer;
       const transition = windowTransitionRef.current;
-      if (hasData) frozenNowRef.current = Date.now() / 1e3 - timeDebtRef.current;
-      const now = useStash ? frozenNowRef.current : Date.now() / 1e3 - timeDebtRef.current;
+      if (hasData) frozenNowRef.current = Date.now() / 1e3 - timeDebtRef.current - (cfg.nowOffset ?? 0);
+      const now = useStash ? frozenNowRef.current : Date.now() / 1e3 - timeDebtRef.current - (cfg.nowOffset ?? 0);
       const windowResult = updateWindowTransition(
         cfg,
         transition,
@@ -3559,6 +3614,9 @@ function Liveline({
   onModeChange,
   onSeriesToggle,
   seriesToggleCompact = false,
+  showSeriesChips = true,
+  yDomain,
+  nowOffset,
   lineWidth,
   className,
   style
@@ -3589,13 +3647,26 @@ function Liveline({
   }, [seriesProp, theme]);
   const multiSeries = (0, import_react2.useMemo)(() => {
     if (!seriesProp || !seriesPalettes) return void 0;
-    return seriesProp.map((s, i) => ({
-      id: s.id,
-      data: s.data,
-      value: s.value,
-      palette: seriesPalettes.get(s.id) ?? resolveTheme(s.color || SERIES_COLORS[i % SERIES_COLORS.length], theme),
-      label: s.label
-    }));
+    return seriesProp.map((s, i) => {
+      const basePalette = seriesPalettes.get(s.id) ?? resolveTheme(s.color || SERIES_COLORS[i % SERIES_COLORS.length], theme);
+      const palette = {
+        ...basePalette,
+        ...(s.lineWidth     != null ? { lineWidth:     s.lineWidth     } : {}),
+        ...(s.lineStyle     != null ? { lineStyle:     s.lineStyle     } : {}),
+        ...(s.gradientStops != null ? { gradientStops: s.gradientStops } : {}),
+        ...(s.dotColor      != null ? { dotFlat:       s.dotColor      } : {}),
+      };
+      return {
+        id: s.id,
+        data: s.data,
+        value: s.value,
+        palette,
+        label: s.label,
+        extendToNow: s.extendToNow !== false,
+        linear: s.linear === true,
+        sessions: s.sessions ?? null
+      };
+    });
   }, [seriesProp, seriesPalettes, theme]);
   const showMomentum = momentum !== false;
   const momentumOverride = typeof momentum === "string" ? momentum : void 0;
@@ -3695,7 +3766,9 @@ function Liveline({
     lineValue,
     multiSeries,
     isMultiSeries,
-    hiddenSeriesIds: hiddenSeries
+    hiddenSeriesIds: hiddenSeries,
+    yDomain,
+    nowOffset
   });
   const cursorStyle = scrub ? cursor : "default";
   const activeColor = isDark ? "rgba(255,255,255,0.7)" : "rgba(0,0,0,0.55)";
@@ -3719,7 +3792,7 @@ function Liveline({
         }
       }
     ),
-    (windows && windows.length > 0 || onModeChange || showSeriesToggle) && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 6, marginBottom: 6, marginLeft: pad.left }, children: [
+    (windows && windows.length > 0 || onModeChange || (showSeriesToggle && showSeriesChips)) && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 6, marginBottom: 6, marginLeft: pad.left }, children: [
       windows && windows.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(
         "div",
         {
@@ -3905,7 +3978,7 @@ function Liveline({
           ]
         }
       ),
-      showSeriesToggle && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: {
+      showSeriesToggle && showSeriesChips && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: {
         display: "inline-flex",
         gap: ws === "text" ? 4 : 2,
         background: ws === "text" ? "transparent" : isDark ? "rgba(255,255,255,0.03)" : "rgba(0,0,0,0.02)",
diff --git a/node_modules/liveline/dist/index.d.cts b/node_modules/liveline/dist/index.d.cts
index 414c4e6..b00858f 100644
--- a/node_modules/liveline/dist/index.d.cts
+++ b/node_modules/liveline/dist/index.d.cts
@@ -45,6 +45,16 @@ interface LivelineSeries {
     value: number;
     color: string;
     label?: string;
+    /** When false, the line ends at the last data point (no horizontal tail to `now`). Default true. */
+    extendToNow?: boolean;
+    /** When true, stroke uses straight segments (no cubic spline). Default false. */
+    linear?: boolean;
+    /** Open-market session windows. When provided the line renders dim outside sessions and full inside, with a gradient fade at each boundary. */
+    sessions?: { start: number; end: number }[];
+    /** Override the stroke width for this individual series (default: palette lineWidth ≈ 2). */
+    lineWidth?: number;
+    /** Override the inner end-dot fill colour (defaults to line colour). */
+    dotColor?: string;
 }
 interface LivelineProps {
     data: LivelinePoint[];
@@ -92,6 +102,8 @@ interface LivelineProps {
     onModeChange?: (mode: 'line' | 'candle') => void;
     onSeriesToggle?: (id: string, visible: boolean) => void;
     seriesToggleCompact?: boolean;
+    showSeriesChips?: boolean;
+    yDomain?: { min: number; max: number };
     className?: string;
     style?: CSSProperties;
 }
diff --git a/node_modules/liveline/dist/index.d.ts b/node_modules/liveline/dist/index.d.ts
index 414c4e6..99538d8 100644
--- a/node_modules/liveline/dist/index.d.ts
+++ b/node_modules/liveline/dist/index.d.ts
@@ -45,6 +45,20 @@ interface LivelineSeries {
     value: number;
     color: string;
     label?: string;
+    /** When false, the line ends at the last data point (no horizontal tail to `now`). Default true. */
+    extendToNow?: boolean;
+    /** When true, stroke uses straight segments (no cubic spline). Default false. */
+    linear?: boolean;
+    /** Open-market session windows. When provided the line renders dim outside sessions and full inside, with a gradient fade at each boundary. */
+    sessions?: { start: number; end: number }[];
+    /** Override the stroke width for this individual series (default: palette lineWidth ≈ 2). */
+    lineWidth?: number;
+    /** Stroke dash pattern: solid (default), dashed, or dotted. */
+    lineStyle?: 'solid' | 'dashed' | 'dotted';
+    /** Two or three colour stops for a horizontal gradient stroke. */
+    gradientStops?: string[];
+    /** Override the inner end-dot fill colour (defaults to line colour). */
+    dotColor?: string;
 }
 interface LivelineProps {
     data: LivelinePoint[];
@@ -92,6 +106,9 @@ interface LivelineProps {
     onModeChange?: (mode: 'line' | 'candle') => void;
     onSeriesToggle?: (id: string, visible: boolean) => void;
     seriesToggleCompact?: boolean;
+    showSeriesChips?: boolean;
+    yDomain?: { min: number; max: number };
+    nowOffset?: number;
     className?: string;
     style?: CSSProperties;
 }
diff --git a/node_modules/liveline/dist/index.js b/node_modules/liveline/dist/index.js
index fd05062..7e12506 100644
--- a/node_modules/liveline/dist/index.js
+++ b/node_modules/liveline/dist/index.js
@@ -360,7 +360,7 @@ function blendColor(c1, c2, t) {
   if (a >= 0.995) return `rgb(${r},${g},${b})`;
   return `rgba(${r},${g},${b},${a.toFixed(3)})`;
 }
-function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlpha = 1, strokeColor) {
+function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlpha = 1, strokeColor, linear = false) {
   const { h, pad } = layout;
   const baseAlpha = ctx.globalAlpha;
   if (showFill && fillAlpha > 0.01) {
@@ -371,7 +371,13 @@ function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlp
     ctx.beginPath();
     ctx.moveTo(pts[0][0], h - pad.bottom);
     ctx.lineTo(pts[0][0], pts[0][1]);
-    drawSpline(ctx, pts);
+    if (linear) {
+      for (let i = 1; i < pts.length; i++) {
+        ctx.lineTo(pts[i][0], pts[i][1]);
+      }
+    } else {
+      drawSpline(ctx, pts);
+    }
     ctx.lineTo(pts[pts.length - 1][0], h - pad.bottom);
     ctx.closePath();
     ctx.fillStyle = grad;
@@ -380,15 +386,99 @@ function renderCurve(ctx, layout, palette, pts, showFill, lineAlpha = 1, fillAlp
   ctx.globalAlpha = baseAlpha * lineAlpha;
   ctx.beginPath();
   ctx.moveTo(pts[0][0], pts[0][1]);
-  drawSpline(ctx, pts);
-  ctx.strokeStyle = strokeColor ?? palette.line;
+  if (linear) {
+    for (let i = 1; i < pts.length; i++) {
+      ctx.lineTo(pts[i][0], pts[i][1]);
+    }
+  } else {
+    drawSpline(ctx, pts);
+  }
+  const _lineDash = palette.lineStyle === 'dashed' ? [8, 5] : palette.lineStyle === 'dotted' ? [2, 4] : [];
+  ctx.setLineDash(_lineDash);
+  if (palette.gradientStops && palette.gradientStops.length >= 2) {
+    const _grad = ctx.createLinearGradient(layout.pad.left, 0, layout.pad.left + layout.chartW, 0);
+    palette.gradientStops.forEach((c, i) => _grad.addColorStop(i / (palette.gradientStops.length - 1), c));
+    ctx.strokeStyle = _grad;
+  } else {
+    ctx.strokeStyle = strokeColor ?? palette.line;
+  }
   ctx.lineWidth = palette.lineWidth;
   ctx.lineJoin = "round";
   ctx.lineCap = "round";
   ctx.stroke();
+  ctx.setLineDash([]);
   ctx.globalAlpha = baseAlpha;
 }
-function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scrubX, scrubAmount = 0, chartReveal = 1, now_ms = 0, colorBlend = 1, skipDashLine = false, fillScale = 1) {
+var SESSION_DIM_ALPHA = 0.35;
+var SESSION_FADE_SECS = 0;
+function renderCurveWithSessions(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor, linear, sessions) {
+  if (!sessions) {
+    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor, linear);
+    return;
+  }
+  const { pad, toX, h, chartH } = layout;
+  const baseAlpha = ctx.globalAlpha;
+  const resolvedColor = strokeColor ?? palette.line;
+  const [r, g, b] = parseRgba(resolvedColor);
+  const dimColor = `rgba(${r},${g},${b},${SESSION_DIM_ALPHA})`;
+  const fullColor = resolvedColor;
+  const fadeW = (toX(SESSION_FADE_SECS) - toX(0));
+  const drawPath = (color, alpha) => {
+    ctx.save();
+    ctx.globalAlpha = baseAlpha * alpha;
+    ctx.beginPath();
+    ctx.moveTo(pts[0][0], pts[0][1]);
+    if (linear) {
+      for (let i = 1; i < pts.length; i++) ctx.lineTo(pts[i][0], pts[i][1]);
+    } else {
+      drawSpline(ctx, pts);
+    }
+    const _dp_dash = palette.lineStyle === 'dashed' ? [8, 5] : palette.lineStyle === 'dotted' ? [2, 4] : [];
+    ctx.setLineDash(_dp_dash);
+    ctx.strokeStyle = color;
+    ctx.lineWidth = palette.lineWidth;
+    ctx.lineJoin = "round";
+    ctx.lineCap = "round";
+    ctx.stroke();
+    ctx.setLineDash([]);
+    ctx.restore();
+  };
+  const clipRect = (x, w, fn) => {
+    ctx.save();
+    ctx.beginPath();
+    ctx.rect(x, pad.top, w, chartH);
+    ctx.clip();
+    fn();
+    ctx.restore();
+  };
+  if (sessions.length === 0) {
+    clipRect(pad.left - 1, layout.w, () => drawPath(dimColor, lineAlpha));
+    return;
+  }
+  clipRect(pad.left - 1, layout.w, () => drawPath(dimColor, lineAlpha));
+  for (const sess of sessions) {
+    const x1 = toX(sess.start);
+    const x2 = toX(sess.end);
+    const coreX1 = x1 + fadeW;
+    const coreX2 = x2 - fadeW;
+    if (coreX1 < coreX2) {
+      clipRect(coreX1, coreX2 - coreX1, () => drawPath(fullColor, lineAlpha));
+    }
+    clipRect(x1 - fadeW, 2 * fadeW, () => {
+      const grad = ctx.createLinearGradient(x1 - fadeW, 0, x1 + fadeW, 0);
+      grad.addColorStop(0, dimColor);
+      grad.addColorStop(1, fullColor);
+      drawPath(grad, lineAlpha);
+    });
+    clipRect(x2 - fadeW, 2 * fadeW, () => {
+      const grad = ctx.createLinearGradient(x2 - fadeW, 0, x2 + fadeW, 0);
+      grad.addColorStop(0, fullColor);
+      grad.addColorStop(1, dimColor);
+      drawPath(grad, lineAlpha);
+    });
+  }
+}
+function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scrubX, scrubAmount = 0, chartReveal = 1, now_ms = 0, colorBlend = 1, skipDashLine = false, fillScale = 1, extendToNow = true, linear = false, sessions = null) {
   const { h, pad, toX, toY, chartW, chartH } = layout;
   const incomingAlpha = ctx.globalAlpha;
   const yMin = pad.top;
@@ -404,15 +494,19 @@ function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scr
     const baseY = loadingY(t, centerY, amplitude, scroll);
     return baseY + (rawY - baseY) * localReveal;
   } : (rawY, _x) => rawY;
+  const doLiveTail = extendToNow !== false;
   const pts = visible.map((p, i) => {
     const x = toX(p.time);
-    const y = i === visible.length - 1 ? morphY(clampY(toY(smoothValue)), x) : morphY(clampY(toY(p.value)), x);
+    const useSmoothLast = doLiveTail && i === visible.length - 1;
+    const y = useSmoothLast ? morphY(clampY(toY(smoothValue)), x) : morphY(clampY(toY(p.value)), x);
     return [x, y];
   });
-  const liveTipX = toX(now);
-  const fullRightX = pad.left + chartW;
-  const tipX = chartReveal < 1 ? liveTipX + (fullRightX - liveTipX) * (1 - chartReveal) : liveTipX;
-  pts.push([tipX, morphY(clampY(toY(smoothValue)), tipX)]);
+  if (doLiveTail) {
+    const liveTipX = toX(now);
+    const fullRightX = pad.left + chartW;
+    const tipX = chartReveal < 1 ? liveTipX + (fullRightX - liveTipX) * (1 - chartReveal) : liveTipX;
+    pts.push([tipX, morphY(clampY(toY(smoothValue)), tipX)]);
+  }
   if (pts.length < 2) return;
   let lineAlpha = 1;
   let fillAlpha = fillScale;
@@ -424,6 +518,7 @@ function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scr
   const colorT = Math.min(1, chartReveal * 3) * colorBlend;
   const strokeColor = chartReveal < 1 || colorBlend < 1 ? blendColor(palette.gridLabel, palette.line, colorT) : void 0;
   const isScrubbing = scrubX !== null;
+  const renderFn = sessions ? (sc, la, fa) => renderCurveWithSessions(ctx, layout, palette, pts, showFill, la, fa, sc, linear, sessions) : (sc, la, fa) => renderCurve(ctx, layout, palette, pts, showFill, la, fa, sc, linear);
   ctx.save();
   ctx.beginPath();
   ctx.rect(pad.left - 1, pad.top, chartW + 2, chartH);
@@ -433,17 +528,17 @@ function drawLine(ctx, layout, palette, visible, smoothValue, now, showFill, scr
     ctx.beginPath();
     ctx.rect(0, 0, scrubX, h);
     ctx.clip();
-    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor);
+    renderFn(strokeColor, lineAlpha, fillAlpha);
     ctx.restore();
     ctx.save();
     ctx.beginPath();
     ctx.rect(scrubX, 0, layout.w - scrubX, h);
     ctx.clip();
     ctx.globalAlpha = incomingAlpha * (1 - scrubAmount * 0.6);
-    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor);
+    renderFn(strokeColor, lineAlpha, fillAlpha);
     ctx.restore();
   } else {
-    renderCurve(ctx, layout, palette, pts, showFill, lineAlpha, fillAlpha, strokeColor);
+    renderFn(strokeColor, lineAlpha, fillAlpha);
   }
   ctx.restore();
   if (!skipDashLine) {
@@ -505,11 +600,12 @@ function drawDot(ctx, x, y, palette, pulse = true, scrubAmount = 0, now_ms = per
   ctx.globalAlpha = baseAlpha;
   ctx.beginPath();
   ctx.arc(x, y, 3.5, 0, Math.PI * 2);
+  const dotFill = palette.dotFlat ?? palette.line;
   if (dim > 0.01) {
-    const lineRgb = parseColorRgb(palette.line);
+    const lineRgb = parseColorRgb(dotFill);
     ctx.fillStyle = lerpColor(lineRgb, outerRgb, dim);
   } else {
-    ctx.fillStyle = palette.line;
+    ctx.fillStyle = dotFill;
   }
   ctx.fill();
 }
@@ -610,6 +706,9 @@ function drawCrosshair(ctx, layout, palette, hoverX, hoverValue, hoverTime, form
   const valueText = formatValue(hoverValue);
   const timeText = formatTime(hoverTime);
   const separator = "  \xB7  ";
+  const vNum = Number(hoverValue);
+  const valueFill =
+    vNum > 0 ? "#22c55e" : vNum < 0 ? "#ef4444" : palette.tooltipText;
   ctx.save();
   ctx.globalAlpha = scrubOpacity;
   ctx.font = '400 13px "SF Mono", Menlo, monospace';
@@ -632,7 +731,7 @@ function drawCrosshair(ctx, layout, palette, hoverX, hoverValue, hoverTime, form
     ctx.strokeText(valueText, tx, ty);
     ctx.strokeText(separator + timeText, tx + valueW, ty);
   }
-  ctx.fillStyle = palette.tooltipText;
+  ctx.fillStyle = valueFill;
   ctx.fillText(valueText, tx, ty);
   ctx.fillStyle = palette.gridLabel;
   ctx.fillText(separator + timeText, tx + valueW, ty);
@@ -661,66 +760,6 @@ function drawMultiCrosshair(ctx, layout, palette, hoverX, hoverTime, entries, fo
       ctx.fill();
     }
   }
-  if (scrubOpacity < 0.1 || layout.w < 300) return;
-  ctx.save();
-  ctx.globalAlpha = scrubOpacity;
-  ctx.font = '400 13px "SF Mono", Menlo, monospace';
-  ctx.textAlign = "left";
-  const timeText = formatTime(hoverTime);
-  const sep = "  \xB7  ";
-  const dotInline = " ";
-  const segments = [
-    { text: timeText, color: palette.gridLabel }
-  ];
-  for (const e of entries) {
-    segments.push({ text: sep, color: palette.gridLabel });
-    segments.push({ text: dotInline, color: e.color, isDot: true });
-    const label = e.label ? `${e.label} ` : "";
-    if (label) segments.push({ text: label, color: palette.gridLabel });
-    segments.push({ text: formatValue(e.value), color: palette.tooltipText });
-  }
-  let totalW = 0;
-  const segWidths = [];
-  for (const seg of segments) {
-    const w = seg.isDot ? 12 : ctx.measureText(seg.text).width;
-    segWidths.push(w);
-    totalW += w;
-  }
-  let tx = hoverX - totalW / 2;
-  const minX = pad.left + 4;
-  const dotRightEdge = liveDotX != null ? liveDotX + 7 : layout.w - pad.right;
-  const maxX = dotRightEdge - totalW;
-  if (tx < minX) tx = minX;
-  if (tx > maxX) tx = maxX;
-  const ty = pad.top + (tooltipY ?? 14) + 10;
-  if (tooltipOutline !== false) {
-    ctx.strokeStyle = palette.tooltipBg;
-    ctx.lineWidth = 3;
-    ctx.lineJoin = "round";
-    let ox2 = tx;
-    for (let i = 0; i < segments.length; i++) {
-      const seg = segments[i];
-      if (!seg.isDot) {
-        ctx.strokeText(seg.text, ox2, ty);
-      }
-      ox2 += segWidths[i];
-    }
-  }
-  let ox = tx;
-  for (let i = 0; i < segments.length; i++) {
-    const seg = segments[i];
-    if (seg.isDot) {
-      ctx.beginPath();
-      ctx.arc(ox + 4, ty - 4, 3.5, 0, Math.PI * 2);
-      ctx.fillStyle = seg.color;
-      ctx.fill();
-    } else {
-      ctx.fillStyle = seg.color;
-      ctx.fillText(seg.text, ox, ty);
-    }
-    ox += segWidths[i];
-  }
-  ctx.restore();
 }
 
 // src/draw/referenceLine.ts
@@ -1324,12 +1363,15 @@ function drawLineModeCrosshair(ctx, layout, palette, hoverX, value, hoverTime, f
   if (opacity < 0.1 || layout.w < 200) return;
   const val = formatValue(value);
   const time = formatTime(hoverTime);
+  const vNum = Number(value);
+  const valCol =
+    vNum > 0 ? "#22c55e" : vNum < 0 ? "#ef4444" : palette.line;
   ctx.save();
   ctx.globalAlpha = opacity;
   ctx.font = '400 13px "SF Mono", Menlo, monospace';
   ctx.textAlign = "left";
   const parts = [
-    { text: val, color: palette.line },
+    { text: val, color: valCol },
     { text: "  \xB7  ", color: palette.gridLabel },
     { text: time, color: palette.gridLabel }
   ];
@@ -1545,7 +1587,7 @@ function drawFrame(ctx, layout, palette, opts) {
     const lastPt = pts[pts.length - 1];
     const distToLive = lastPt[0] - opts.hoverX;
     const fadeStart = Math.min(80, layout.chartW * 0.3);
-    const scrubOpacity = distToLive < CROSSHAIR_FADE_MIN_PX ? 0 : distToLive >= fadeStart ? opts.scrubAmount : (distToLive - CROSSHAIR_FADE_MIN_PX) / (fadeStart - CROSSHAIR_FADE_MIN_PX) * opts.scrubAmount;
+    const scrubOpacity = opts.scrubAmount;
     if (scrubOpacity > 0.01) {
       drawCrosshair(
         ctx,
@@ -1600,6 +1642,9 @@ function drawMultiFrame(ctx, layout, opts) {
     if (combinedAlpha < 0.01) continue;
     ctx.save();
     if (combinedAlpha < 1) ctx.globalAlpha = combinedAlpha;
+    const ext = s.extendToNow !== false;
+    const lin = s.linear === true;
+    const sess = s.sessions ?? null;
     const pts = drawLine(
       ctx,
       layout,
@@ -1612,7 +1657,13 @@ function drawMultiFrame(ctx, layout, opts) {
       scrubX,
       opts.scrubAmount,
       reveal,
-      opts.now_ms
+      opts.now_ms,
+      1,
+      true,
+      1,
+      ext,
+      lin,
+      sess
     );
     ctx.restore();
     if (pts && pts.length > 0) {
@@ -1634,17 +1685,18 @@ function drawMultiFrame(ctx, layout, opts) {
     for (const entry of allPts) {
       if (entry.alpha < 0.01) continue;
       const lastPt = entry.pts[entry.pts.length - 1];
+      const dotFill = entry.palette.dotFlat ?? entry.palette.line;
       ctx.save();
       ctx.globalAlpha = dotAlpha * entry.alpha;
       if (showPulse && entry.alpha > 0.5) {
-        drawMultiDot(ctx, lastPt[0], lastPt[1], entry.palette.line, true, opts.now_ms, 3);
+        drawMultiDot(ctx, lastPt[0], lastPt[1], dotFill, true, opts.now_ms, 3);
       } else {
-        drawSimpleDot(ctx, lastPt[0], lastPt[1], entry.palette.line, 3);
+        drawSimpleDot(ctx, lastPt[0], lastPt[1], dotFill, 3);
       }
       if (entry.label) {
         ctx.font = '600 10px -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif';
         ctx.textAlign = "left";
-        ctx.fillStyle = entry.palette.line;
+        ctx.fillStyle = dotFill;
         ctx.fillText(entry.label, lastPt[0] + 6, lastPt[1] + 3.5);
       }
       ctx.restore();
@@ -1667,7 +1719,7 @@ function drawMultiFrame(ctx, layout, opts) {
     }
     const distToLive = maxLiveDotX - opts.hoverX;
     const fadeStart = Math.min(80, layout.chartW * 0.3);
-    const scrubOpacity = distToLive < CROSSHAIR_FADE_MIN_PX ? 0 : distToLive >= fadeStart ? opts.scrubAmount : (distToLive - CROSSHAIR_FADE_MIN_PX) / (fadeStart - CROSSHAIR_FADE_MIN_PX) * opts.scrubAmount;
+    const scrubOpacity = opts.scrubAmount;
     if (scrubOpacity > 0.01) {
       drawMultiCrosshair(
         ctx,
@@ -1963,8 +2015,8 @@ var VALUE_SNAP_THRESHOLD = 1e-3;
 var ADAPTIVE_SPEED_BOOST = 0.2;
 var MOMENTUM_GREEN = [34, 197, 94];
 var MOMENTUM_RED = [239, 68, 68];
-var CHART_REVEAL_SPEED = 0.14;
-var CHART_REVEAL_SPEED_FWD = 0.09;
+var CHART_REVEAL_SPEED = 1;
+var CHART_REVEAL_SPEED_FWD = 1;
 var PAUSE_PROGRESS_SPEED = 0.12;
 var PAUSE_CATCHUP_SPEED = 0.08;
 var PAUSE_CATCHUP_SPEED_FAST = 0.22;
@@ -2584,7 +2636,10 @@ function useLivelineEngine(canvasRef, containerRef, config) {
           data: s.data.slice(),
           value: s.value,
           palette: s.palette,
-          label: s.label
+          label: s.label,
+          extendToNow: s.extendToNow !== false,
+          linear: s.linear === true,
+          sessions: s.sessions ?? null
         }));
       }
       if (hasData && !cfg.isMultiSeries) lastMultiSeriesRef.current = [];
@@ -2633,8 +2688,8 @@ function useLivelineEngine(canvasRef, containerRef, config) {
     }
     if (isCandle) {
       const candleBuffer = CANDLE_BUFFER_NO_BADGE;
-      if (hasData) frozenNowRef.current = Date.now() / 1e3 - timeDebtRef.current;
-      const now = hasData || chartReveal < 5e-3 ? Date.now() / 1e3 - timeDebtRef.current : frozenNowRef.current;
+      if (hasData) frozenNowRef.current = Date.now() / 1e3 - timeDebtRef.current - (cfg.nowOffset ?? 0);
+      const now = hasData || chartReveal < 5e-3 ? Date.now() / 1e3 - timeDebtRef.current - (cfg.nowOffset ?? 0) : frozenNowRef.current;
       const rawLive = pausedCandlesRef.current ? pausedLiveRef.current ?? void 0 : cfg.liveCandle;
       let effectiveLineData = pausedLineDataRef.current ?? cfg.lineData;
       let effectiveLineValue = pausedLineValueRef.current ?? cfg.lineValue;
@@ -3060,8 +3115,8 @@ function useLivelineEngine(canvasRef, containerRef, config) {
       }
       const firstSeries = effectiveMultiSeries[0];
       const transition = windowTransitionRef.current;
-      if (hasData) frozenNowRef.current = Date.now() / 1e3 - timeDebtRef.current;
-      const now = useMultiStash ? frozenNowRef.current : Date.now() / 1e3 - timeDebtRef.current;
+      if (hasData) frozenNowRef.current = Date.now() / 1e3 - timeDebtRef.current - (cfg.nowOffset ?? 0);
+      const now = useMultiStash ? frozenNowRef.current : Date.now() / 1e3 - timeDebtRef.current - (cfg.nowOffset ?? 0);
       const smoothValues = /* @__PURE__ */ new Map();
       for (const s of effectiveMultiSeries) {
         let dv = displayValuesRef.current.get(s.id);
@@ -3154,7 +3209,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
             if (range.min < globalMin) globalMin = range.min;
             if (range.max > globalMax) globalMax = range.max;
           }
-          seriesEntries.push({ visible, smoothValue: sv, palette: s.palette, label: s.label, alpha });
+          seriesEntries.push({ visible, smoothValue: sv, palette: s.palette, label: s.label, alpha, extendToNow: s.extendToNow !== false, linear: s.linear === true, sessions: s.sessions ?? null });
         }
       }
       if (seriesEntries.length === 0) {
@@ -3176,7 +3231,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
         rafRef.current = requestAnimationFrame(draw);
         return;
       }
-      const computedRange = { min: isFinite(globalMin) ? globalMin : 0, max: isFinite(globalMax) ? globalMax : 1 };
+      const computedRange = cfg.yDomain ? { min: cfg.yDomain.min, max: cfg.yDomain.max } : { min: isFinite(globalMin) ? globalMin : 0, max: isFinite(globalMax) ? globalMax : 1 };
       const adaptiveSpeed = cfg.lerpSpeed + ADAPTIVE_SPEED_BOOST * 0.5;
       const rangeResult = updateRange(
         computedRange,
@@ -3305,8 +3360,8 @@ function useLivelineEngine(canvasRef, containerRef, config) {
       const needsArrowRoom = cfg.showMomentum && cfg.showBadge;
       const buffer = needsArrowRoom ? Math.max(baseBuffer, 37 / Math.max(chartW, 1)) : baseBuffer;
       const transition = windowTransitionRef.current;
-      if (hasData) frozenNowRef.current = Date.now() / 1e3 - timeDebtRef.current;
-      const now = useStash ? frozenNowRef.current : Date.now() / 1e3 - timeDebtRef.current;
+      if (hasData) frozenNowRef.current = Date.now() / 1e3 - timeDebtRef.current - (cfg.nowOffset ?? 0);
+      const now = useStash ? frozenNowRef.current : Date.now() / 1e3 - timeDebtRef.current - (cfg.nowOffset ?? 0);
       const windowResult = updateWindowTransition(
         cfg,
         transition,
@@ -3532,6 +3587,9 @@ function Liveline({
   onModeChange,
   onSeriesToggle,
   seriesToggleCompact = false,
+  yDomain,
+  nowOffset,
+  showSeriesChips = true,
   lineWidth,
   className,
   style
@@ -3562,13 +3620,26 @@ function Liveline({
   }, [seriesProp, theme]);
   const multiSeries = useMemo(() => {
     if (!seriesProp || !seriesPalettes) return void 0;
-    return seriesProp.map((s, i) => ({
-      id: s.id,
-      data: s.data,
-      value: s.value,
-      palette: seriesPalettes.get(s.id) ?? resolveTheme(s.color || SERIES_COLORS[i % SERIES_COLORS.length], theme),
-      label: s.label
-    }));
+    return seriesProp.map((s, i) => {
+      const basePalette = seriesPalettes.get(s.id) ?? resolveTheme(s.color || SERIES_COLORS[i % SERIES_COLORS.length], theme);
+      const palette = {
+        ...basePalette,
+        ...(s.lineWidth     != null ? { lineWidth:     s.lineWidth     } : {}),
+        ...(s.lineStyle     != null ? { lineStyle:     s.lineStyle     } : {}),
+        ...(s.gradientStops != null ? { gradientStops: s.gradientStops } : {}),
+        ...(s.dotColor      != null ? { dotFlat:       s.dotColor      } : {}),
+      };
+      return {
+        id: s.id,
+        data: s.data,
+        value: s.value,
+        palette,
+        label: s.label,
+        extendToNow: s.extendToNow !== false,
+        linear: s.linear === true,
+        sessions: s.sessions ?? null
+      };
+    });
   }, [seriesProp, seriesPalettes, theme]);
   const showMomentum = momentum !== false;
   const momentumOverride = typeof momentum === "string" ? momentum : void 0;
@@ -3668,7 +3739,9 @@ function Liveline({
     lineValue,
     multiSeries,
     isMultiSeries,
-    hiddenSeriesIds: hiddenSeries
+    hiddenSeriesIds: hiddenSeries,
+    yDomain,
+    nowOffset
   });
   const cursorStyle = scrub ? cursor : "default";
   const activeColor = isDark ? "rgba(255,255,255,0.7)" : "rgba(0,0,0,0.55)";
@@ -3692,7 +3765,7 @@ function Liveline({
         }
       }
     ),
-    (windows && windows.length > 0 || onModeChange || showSeriesToggle) && /* @__PURE__ */ jsxs("div", { style: { display: "flex", alignItems: "center", gap: 6, marginBottom: 6, marginLeft: pad.left }, children: [
+    (windows && windows.length > 0 || onModeChange || (showSeriesToggle && showSeriesChips)) && /* @__PURE__ */ jsxs("div", { style: { display: "flex", alignItems: "center", gap: 6, marginBottom: 6, marginLeft: pad.left }, children: [
       windows && windows.length > 0 && /* @__PURE__ */ jsxs(
         "div",
         {
@@ -3878,7 +3951,7 @@ function Liveline({
           ]
         }
       ),
-      showSeriesToggle && /* @__PURE__ */ jsx("div", { style: {
+      showSeriesToggle && showSeriesChips && /* @__PURE__ */ jsx("div", { style: {
         display: "inline-flex",
         gap: ws === "text" ? 4 : 2,
         background: ws === "text" ? "transparent" : isDark ? "rgba(255,255,255,0.03)" : "rgba(0,0,0,0.02)",
