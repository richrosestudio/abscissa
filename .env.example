# Optional: where Vite proxies /api during `npm run dev`.
# Default in vite.config.ts is https://abscissa.live so local UI matches production data.
# Uncomment when using local API:
# VITE_API_PROXY_TARGET=http://localhost:3000
